<?php

if (! function_exists('jpostal_pref')) {
    /**
     * Assign high numeric IDs to a config item to force appending.
     *
     * @return array
     */
    function jpostal_pref()
    {
        dump(__METHOD__);
    }
}
